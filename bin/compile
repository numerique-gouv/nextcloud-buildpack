#!/usr/bin/env bash
# bin/compile <build-dir> <cache-dir>

basedir="$( cd -P "$( dirname "$0" )" && pwd )" 


echo "=====> Start building Nextcloud"

BUILD_DIR=$1
CACHE_DIR=$2
ENV_DIR=${3:-}

echo $basedir
